apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: node-exporter
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: node-exporter
  values:
    imagePullSecrets[0].name: dhi-registry
