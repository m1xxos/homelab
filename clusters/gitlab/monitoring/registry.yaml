apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: node-exporter
spec:
  interval: 10m
  layerSelector:
    mediaType: "application/vnd.cncf.helm.chart.content.v1.tar+gzip"
    operation: copy
  url: oci://dhi.io/node-exporter-chart
  ref:
    semver: ">= 4.50.0"
  secretRef:
    name: dhi-registry
