apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-core-secret
  namespace: harbor
spec:
  refreshInterval: "1h0m0s"
  secretStoreRef:
    name: harbor-store
  target:
    name: harbor-core-secret
  data:
  - secretKey: secretKey
    remoteRef:
      key: harbor/core
      property: secretKey
