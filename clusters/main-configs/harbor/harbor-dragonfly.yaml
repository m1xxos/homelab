apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: harbor-dragonfly
  namespace: harbor
spec:
  replicas: 1
  authentication:
    passwordFromSecret:
      name: harbor-redis
      key: password
