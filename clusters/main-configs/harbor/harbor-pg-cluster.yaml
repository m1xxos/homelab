apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: harbor-pg
  namespace: harbor
spec:
  instances: 1
  storage:
    size: 5Gi
  bootstrap:
    initdb:
      database: harbor
      owner: harbor
