apiVersion: operator.victoriametrics.com/v1
kind: VLSingle
metadata:
  name: main
spec:
  retentionPeriod: "3d"
  resources:
    requests:
      cpu: 20m
      memory: 128Mi
    limits:
      memory: 256Mi
  # Logs are disposable (3d retention) — no need for a second Longhorn replica.
  # Changing this on a live cluster requires deleting the old PVC.
  storage:
    storageClassName: longhorn-single
    resources:
      requests:
        storage: 10Gi
  serviceSpec:
    metadata:
      annotations:
        service.cilium.io/global: "true"
    spec: {}
    useAsDefault: true
