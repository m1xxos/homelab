apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: full-trim
  namespace: longhorn-system
spec:
  cron: "45 */6 * * *"
  task: "filesystem-trim"
  groups:
  - default
  retain: 1
  concurrency: 1
