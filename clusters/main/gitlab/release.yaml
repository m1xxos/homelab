---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitlab
  namespace: gitlab
spec:
  interval: 30m
  chart:
    spec:
      chart: gitlab
      version: 9.10.1
      sourceRef:
        kind: HelmRepository
        name: gitlab
  valuesFrom:
  - kind: ConfigMap
    name: gitlab-values
    valuesKey: values.yaml
