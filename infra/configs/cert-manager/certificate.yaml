apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: local-m1xxos
  namespace: traefik
spec:
  secretName: local-m1xxos
  dnsNames:
  - "${DNS_NAME}"
  - "*.${DNS_NAME}"
  issuerRef:
    name: acme-issuer
    kind: ClusterIssuer
